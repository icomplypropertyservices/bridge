# Server-only secrets (set in Vercel project env — never commit)
XRPL_TO_API_KEY=your_bridge_api_key
PLATFORM_FEE_BPS=85

# Client-side (bundled into the build — not a secret, but required)
# Create a project at https://dashboard.reown.com and add your deployed domain.
# One Reown id drives every WalletConnect surface: EVM, Solana, XRPL and the
# wallet-registry lookup. Reown is WalletConnect — a second id only drifts.
VITE_REOWN_PROJECT_ID=your_reown_project_id

# Optional Solana RPC override; defaults to Reown's Blockchain API
# VITE_SOLANA_RPC_URL=
